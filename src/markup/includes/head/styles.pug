= '\n'

// Styles
link(type='text/css', charset='utf-8', rel='stylesheet', media='all', href='/assets/css/' + labels.lang + '.css')/

if labels.lang === 'bg'
  link(href='https://fonts.googleapis.com/css?family=PT+Sans:400,400i,700,700i', rel='stylesheet')/
